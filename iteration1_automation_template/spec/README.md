# spec 目录

请把项目统一的地图文件放到这里，并命名为 `map.json`。

`map.json` 必须由团队统一确定，至少包含：

- 70 个位置（编号 0..69）的类型；
- 普通地产的地段和价格；
- 特殊格位置：起点、道具屋、礼品屋、魔法屋、医院、监狱、矿地；
- 每个矿地对应的点数。

测试用例里的 `"map_file": "map.json"` 指向的就是这个文件。
